\RequirePackage{../../../course/course-specific/course-config}
\RequirePackage{../../../base/commands}
\RequirePackage{../../../base/head}
\RequirePackage{../../../base/cover}

% SUBJECT-SPECIFIC
\RequirePackage{listings}
\RequirePackage{lstautogobble}
\RequirePackage{lstmisc}

% LSTSTYLE
\definecolor{codered}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}
\definecolor{keyword}{rgb}{0.55294118, 0.2745098, 1}

\lstdefinestyle{lststyle}{
    commentstyle= \color{codegray},
    keywordstyle= \ttfamily\itshape\color{codeblue},
    numberstyle= \footnotesize\color{codegray},
    basicstyle= \ttfamily\upshape,
    backgroundcolor= \color{backcolour},
    stringstyle= \ttfamily\color{codepurple},
    breakatwhitespace= false,
    autogobble= true,
    breaklines= true,
    captionpos= b,
    keepspaces= true,
    numbers= false,
    numbersep= 4pt,
    showspaces= false,
    showstringspaces= false,
    showtabs= true,
    tabsize= 4,
}
\lstset{style=lststyle}

% PROOF (+ MATH. INDUCTION)
\newif\ifinbeweis
\newcommand{\proofsection}[2]{%
    \ifinbeweis
    \newline \reversemarginpar \marginnote{\textsf{(I.B.)}} \reversemarginpar \textsf{\textbf{Induktionsbeginn. }}
    \else
    \newline \textbf{Fehler:} Dieser Befehl kann nur innerhalb des 'beweis'-Environments verwendet werden.
    \fi
}

\newcommand{\IB}{%
    \ifinbeweis
    \newline \reversemarginpar \marginnote{\textsf{(I.B.)}} \reversemarginpar \textsf{\textbf{Induktionsbeginn. }}
    \else
    \newline \textbf{Fehler:} Dieser Befehl kann nur innerhalb des 'beweis'-Environments verwendet werden.
    \fi
}

\newcommand{\IA}{%
    \ifinbeweis
    \newline \reversemarginpar \marginnote{\textsf{(I.A.)}} \reversemarginpar \textsf{\textbf{Induktionsannahme. }}
    \else
    \newline \textbf{Fehler:} Dieser Befehl kann nur innerhalb des 'beweis'-Environments verwendet werden.
    \fi
}

\newcommand{\IS}[1][\hspace{-2pt}]{%
    \ifinbeweis
    \newline \reversemarginpar \marginnote{\textsf{(I.S. #1)}} \reversemarginpar \textsf{\textbf{Induktionsschritt #1. }}
    \else
    \newline \textbf{Fehler:} Dieser Befehl kann nur innerhalb des 'beweis'-Environments verwendet werden.
    \fi
}